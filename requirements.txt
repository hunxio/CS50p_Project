# To install the libraries run : pip install -r requirements.txt
# On Mac the command might slightly change : pip3 install -r requirements.txt

# GUI
tk
pillow

# API
requests
openmeteo-requests
requests-cache 
retry-requests 
numpy 
pandas
geopy

# Unit tests
pytest